# Exact pins installed by daemon/run.sh. Ranges are rejected.
# numpy 2.5.3 requires Python >= 3.12. Accepted wheel hashes are in
# requirements.lock. The installer refuses sdists and any unlisted artifact.
numpy==2.5.3
